const header=document.querySelector(".header");let lastScroll;lastScroll=0;const getScrollPosition=()=>window.pageYOffset,onScrollShowHeader=()=>{getScrollPosition()>lastScroll&&!header.classList.contains("header--hidden-on-scroll")?header.classList.add("header--hidden-on-scroll"):getScrollPosition()<lastScroll&&header.classList.contains("header--hidden-on-scroll")&&header.classList.remove("header--hidden-on-scroll"),lastScroll=getScrollPosition()};document.addEventListener("scroll",onScrollShowHeader);